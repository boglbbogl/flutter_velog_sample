import 'package:flutter/material.dart';
import 'package:flutter_velog_sample/_core/app_bar.dart';

class DynamicLinksArgumentScreen extends StatelessWidget {
  const DynamicLinksArgumentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: appBar(title: "Argument Dynamic Links"),
    );
  }
}
