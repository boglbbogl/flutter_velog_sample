import 'package:flutter/material.dart';

class ImagePerformancesScreen extends StatelessWidget {
  const ImagePerformancesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
